# Prompt 01 — Architecture CURRENT

Hãy dùng Archify để trả lời: “App Tiệm bánh Mây hiện có những component nào và
chúng gọi nhau ra sao?”

Phạm vi evidence: `public/app.js`, `src/http.js`, `src/catalog.js`,
`src/orders.js`, `src/payment.js`, `src/kitchen.js`, `src/events.js` và tests liên
quan. Trước khi vẽ, liệt kê component/relationship kèm source path. Không thêm
database, message broker, cloud, auth hoặc payment provider thật. Gắn nhãn
CURRENT, dùng tiếng Việt, `quality_profile: showcase`, validate 9/9, deliver HTML
và chạy visual-check. Lưu JSON/HTML/receipt trong `diagrams/current/`.
