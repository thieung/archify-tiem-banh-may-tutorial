# Prompt 03 — Sequence CURRENT

Hãy dùng Archify trả lời: “Khi checkout thành công, Browser, HTTP API, Order
Store, Catalog, Payment Mock và Event Log gọi nhau theo thứ tự nào?”

Đọc `public/app.js`, `src/http.js`, `src/orders.js`, `src/catalog.js`,
`src/payment.js`, `src/events.js`, HTTP tests và happy-path evidence. Chỉ vẽ create
order + payment success để sequence còn đọc được; không thêm database hay network
provider. Dùng tiếng Việt, validate showcase 9/9, deliver và visual-check.
